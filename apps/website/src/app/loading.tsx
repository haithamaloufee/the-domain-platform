import { Container, LoadingLogo, Section } from "@the-domain/ui";
export default function Loading() {
  return (
    <Section>
      <Container>
        <LoadingLogo />
      </Container>
    </Section>
  );
}

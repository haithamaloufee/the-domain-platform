# Public website origin is browser-visible and controls canonicals, robots, and sitemap URLs.
# Use the final HTTPS origin in production. Do not include a path.
NEXT_PUBLIC_WEBSITE_URL=http://localhost:3000
# Server-only API origin. Never rename this to a NEXT_PUBLIC variable.
THE_DOMAIN_API_BASE_URL=http://localhost:5000

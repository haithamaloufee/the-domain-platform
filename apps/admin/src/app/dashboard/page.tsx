import { AdminPlaceholder } from "@/components/layout/admin-placeholder";
export default function DashboardPage() {
  return (
    <AdminPlaceholder
      title="Dashboard overview"
      description="The protected operations shell is ready for authenticated metrics and workflow summaries in a later sprint."
    />
  );
}

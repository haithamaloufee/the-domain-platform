import { AdminPlaceholder } from "@/components/layout/admin-placeholder";
export default function Page() {
  return (
    <AdminPlaceholder
      title="Events management"
      description="Event publishing workflows will be connected to the existing backend in a later sprint."
    />
  );
}

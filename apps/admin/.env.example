# Server-only BFF configuration. Do not prefix server-only values with NEXT_PUBLIC.
THE_DOMAIN_API_BASE_URL=http://localhost:5000
THE_DOMAIN_REFRESH_COOKIE_MAX_AGE_SECONDS=604800

# Browser-visible configuration for future non-authenticated API reads.
# Never place secrets in NEXT_PUBLIC variables.
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

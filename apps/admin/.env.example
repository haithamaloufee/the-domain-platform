# Server-only BFF configuration. Do not prefix these values with NEXT_PUBLIC.
# Use the internal or public HTTPS API origin in production.
THE_DOMAIN_API_BASE_URL=http://localhost:5000
# Keep this aligned with Authentication__RefreshTokenLifetimeDays (14 days by default).
THE_DOMAIN_REFRESH_COOKIE_MAX_AGE_SECONDS=1209600

# Copy required values into local, untracked environment files when apps are scaffolded.
# Never commit credentials or production secrets.

NODE_ENV=development
WEBSITE_URL=http://localhost:3000
ADMIN_URL=http://localhost:3001
API_URL=http://localhost:5000

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=the_domain
POSTGRES_USER=the_domain
POSTGRES_PASSWORD=replace_locally

# ASP.NET Core configuration overrides. Keep the real value in an untracked local environment.
Database__Enabled=false
Database__ConnectionString=
Database__CommandTimeoutSeconds=30

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

Cloudinary__Enabled=false
Cloudinary__CloudName=
Cloudinary__ApiKey=
Cloudinary__ApiSecret=
Cloudinary__FolderPrefix=the-domain
Cloudinary__MaxImageSizeMb=15
Cloudinary__MaxVideoSizeMb=100
# MIME arrays can be overridden with indexed ASP.NET configuration keys when needed.

JWT_ISSUER=
JWT_AUDIENCE=
JWT_SIGNING_KEY=

Authentication__Enabled=false
Authentication__Issuer=TheDomain.Api.Local
Authentication__Audience=TheDomain.Admin.Local
Authentication__SigningKey=
Authentication__AccessTokenLifetimeMinutes=15
Authentication__RefreshTokenLifetimeDays=14

InitialAdmin__Enabled=false
InitialAdmin__Email=
InitialAdmin__Password=
InitialAdmin__FullName=

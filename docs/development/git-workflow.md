# Git workflow

Use focused branches and small, reviewable commits. Never commit secrets, production media, generated build output, local database volumes, or unrelated formatting changes. Pull requests should describe scope, verification, risk, and any deferred work.

# Asset policy

Production media does not belong in this repository. Store production binaries in Cloudinary or approved object storage and persist only metadata and URLs in the database.

`sample-media` may contain a very small set of lightweight, license-safe files for local UI testing. `raw-media` is ignored and exists only as a local staging location.
